[pytest]
# Discover tests across the Python tooling. Each tests/ dir carries a conftest.py that puts its
# package dir on sys.path, so `import reslib` / `import packer` etc. resolve without installation.
testpaths = scripts sw
python_files = test_*.py
addopts = -q

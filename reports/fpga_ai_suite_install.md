# FPGA AI Suite native-Windows installation

Date: 2026-08-19 (America/Chicago)

FPGA AI Suite 2026.1.1 is installed and verified on native Windows. No WSL or
Docker was used. No separate example design was downloaded.

## Installed components

- FPGA AI Suite: `C:\altera_pro\2026.1.1`, registered as version 26.1.1.130.
- DLA compiler: `2026.1.1+b17` (`build_version.txt` is `2026.1.1/17`).
- OpenVINO runtime: `C:\altera_pro\openvino_2025.4.0`.
- Existing Quartus Pro root: `C:\altera_pro\26.1\quartus`.
- The compiler/toolchain runs, but the separate CoreDLA hardware feature
  `6AF7_018B` is not present in either configured license file.

Use `scripts\ai_suite_env.cmd` from a Command Prompt to configure OpenVINO,
FPGA AI Suite, and `QUARTUS_ROOTDIR` in the required order.

## Official downloads and verification

The initial Quartus 26.1 build-110 qinst was audited first. Its CLI rejects
`fpga_ai_suite`, and the matching Altera download page does not offer the suite
as an add-on. The correct official source is the Quartus Pro 26.1.1 Windows
download page.

- Quartus 26.1.1 qinst: `qinst-windows-26.1.1-130.exe`, 40,126,584 bytes;
  published SHA-1
  `D9AFE56BDC78C64581328C8F3D8D23E27A6098D1`; SHA-256
  `D8F9F345F3CC13A6CC29B7AAA83BEDDCB068586EFAF11C5652C0D41FD3EECCE5`;
  Authenticode valid, Intel Corporation.
- FPGA AI Suite installer: `fpga_ai_suite-26.1.1.130-windows.exe`, 55,551,112
  bytes; published SHA-1
  `69A3035A953349C1D81D348B866FB206697A0D66`; SHA-256
  `FAAED83A886299243314DFAC5704F183234AB325481819FBEEA2BC4E10704C56`;
  Authenticode valid, Intel Corporation.
- Official OpenVINO archive:
  `openvino_toolkit_windows_2025.4.0.20398.8fdad55727d_x86_64.zip`,
  131,657,635 bytes; official SHA-256
  `995A88DC1E34DF841CC4DB5AB118A87147608C3E4B67F7D9D86BEF1B311A273E`.

The FPGA AI Suite-only download took `2.3903406` seconds, its unattended
installation took `101.5837781` seconds, the OpenVINO download took
`5.3334559` seconds, and OpenVINO extraction/placement took `1.2207271`
seconds.

## Verification

After both environment scripts were loaded, `dla_compiler --version` returned
`2026.1.1+b17` with exit code 0.

The official bundled Agilex-3 architecture
`AGX3_Small_Softmax.arch` was used only for an installation sanity check:

```text
dla_compiler --fanalyze-area --march AGX3_Small_Softmax.arch
ALMs:        23610
ALUTs:       27285
DSPs:        42
Registers:   68752
M20Ks:       190
Memory ALMs: 2629
```

The command exited 0 in `1.5573449` seconds. Its transient
`compiled_model_dir` output was removed from the Git workspace after the
numeric results were copied here and moved to the recoverable task-temporary
path `C:\Temp\codex-fpga-ai-area-verification`; it can also be regenerated by
rerunning the command. This check validates the compiler installation and
Agilex-3 architecture support. Subsequent work compiled and ran the ResNet-8
RTL path, as documented in `reports/fpga_ai_rtl_run.md`.

An earlier read-only installation audit verified the registry entry,
environment script, compiler/build versions, local artifact hashes and
signatures, area-report values, Windows limitation, and HyperRAM constraint.
Its GO verdict applied to tool installation, not licensed hardware-IP
generation. The later exact feature check showed that `6AF7_018B` is absent;
`dla_create_ip` selected inference-limited RTL and hardware inference failed
the correctness gate.

Native Windows RTL simulation is not supported in FPGA AI Suite 2026.1.1.
Future IP/example generation must use `--skip-sim-env`; Quartus compilation and
hardware execution remain available on Windows.

## Installation incident disclosure

The older qinst GUI was briefly launched while testing its component support.
Its default selection immediately started a 21.05 GB Quartus/device download.
It was stopped at 5%. Several partial/current-timestamp `.qdz` files were
observed under `C:\Users\tcove\Downloads\altera_pro_26.1`. Because that folder
may contain user-owned material, none of those files were deleted or modified
after the process was stopped. The subsequent supported CLI installation
downloaded only FPGA AI Suite into a task-specific temporary directory.

## Scope

The toolchain enables model conversion, compiler analysis, IP generation, and
Quartus integration. The pinned ResNet-8 graph has been compiled wholly to
FPGA, integrated, fit, programmed, and exercised on the board. A valid
CoreDLA hardware license is now required to replace the inference-limited
streamer and pass the 100 MHz correctness gate. Only then should wider cores
and 200/300 MHz trials proceed. HyperRAM remains inactive at 0 MHz.

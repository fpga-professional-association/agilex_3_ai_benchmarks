# Model-zoo deps (issue #2). Heavy — not installed by CI (AGENTS.md); pytest here skips when an
# import is missing. Pinned to what was verified working together on Python 3.12.
numpy==1.26.4
requests>=2.31
tqdm>=4.66
pillow>=10.0

# TF/Keras stack for the MLPerf Tiny four (checkpoints are TFLite; tf2onnx converts them).
tensorflow==2.16.1
tensorflow-datasets==4.9.4
tensorflow-metadata==1.15.0
protobuf==4.25.9
pydub>=0.25  # tfds 'speech_commands' builder needs this for background-noise/silence synthesis
tf2onnx==1.17.0
onnx==1.16.1
onnxruntime>=1.17

# PyTorch stack for MobileNetV2 / ResNet-18 (torchvision pretrained + ONNX export).
torch>=2.2
torchvision>=0.17

# Eval metrics / feature extraction.
scikit-learn>=1.3
librosa>=0.10
pyarrow>=14  # resnet8: read CIFAR-10 test set from a Parquet mirror (lossless PNG rows)

# Quantization pipeline (issue #3). Pinned to OpenVINO 2024.6 (AI Suite 25.x's tracked version,
# PLAN §9 PH2 — do not float this) + the NNCF release contemporaneous with it. Do not upgrade
# either "because newer is better" (AGENTS.md/issue #3: the IR must match what dla_compiler
# in AI Suite 25.x consumes).
openvino==2024.6.0
nncf==2.14.1
